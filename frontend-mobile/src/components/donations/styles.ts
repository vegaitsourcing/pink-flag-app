import styled from '@emotion/native';

export const DonationTitile = styled.Text`
  color: ${({ theme }) => theme.colors.primary};
  font-size: ${({ theme }) => theme.fontSize.$7};
  font-weight: 700;
`;
